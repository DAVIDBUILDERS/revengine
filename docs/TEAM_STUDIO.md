# Team studio — September 13

Your Team now opens a working surface with the company objective, a compact selected-team strip, and a dedicated agent/output area. Selection changes the workbench without opening a drawer. Saved outputs are rendered from real workspace artifacts, with a selector for earlier outputs; missing work is explicitly empty. Preparation uses the existing command, and customer actions still open the reviewed customer workflow. Sources, permissions, history and output reviews remain in the existing detail workspace. When a destination such as commerce or social is not connected, the work product includes copy and download so the client can take it into their own CMS or tools. Copy-out is complete delivery, not a blocker.

The full catalog/recommendation/settings interface is available under Build your team. It is not the default view. The design preserves David typography and red accent with a charcoal objective panel, compact specialist controls and a document surface. It does not simulate work, add a model, integrate Firecrawl, or enable unimplemented capabilities. Other product screens retain their current design.

Checks: 14 Chromium UI journeys passed; lint/module boundaries, type checking and web production build passed. Desktop visual inspection and mobile overflow check completed using synthetic fixtures. Hosted routing verification and deployment records follow the release process.

Hosted verification passed 12 routing/isolation checks with two separate test accounts. Test deployment: Ex97WnVWYESCK6L7XHnf9sjThvc4. Production deployment: AJt8xVQv7zpbDn3TssA2H4jmXpBz. Application source is shared; database and credentials remain isolated. No migration or new provider access was introduced.

## Agent assembly — September 13
Replaced the legacy team configuration form with an objective surface, numbered selected lineup, searchable category-filtered specialist library, capability labels and persistent save bar. All 32 specialists remain inspectable through Role & readiness. Company/source/permission/readiness setup stays available through a disclosure rather than dominating selection.

The builder mounts on first use and remains mounted while returning to the workbench, retaining unsaved selections during that switch. Reloading still restores only saved server state. Selection and recommendation do not authorize execution; saving uses the existing workspace revision check. Applicability and configured allowance remain enforced.

Validation: typecheck, lint/module boundaries and production build passed. Fourteen affected Chromium scenarios passed across runs; an unrelated CSV browser teardown timeout passed on retry. Added draft-retention and mobile overflow assertions. Reviewed desktop/mobile fixture screenshots in ignored artifacts/team-builder-*.png. No new agent capabilities or external integrations are represented as implemented.
